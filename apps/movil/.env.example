# Por defecto la app usa datos de ejemplo (mock), sin necesitar ningún servidor corriendo.
# Para conectarla a apps/api real: copiar este archivo a .env.local y cambiar a "api".
EXPO_PUBLIC_DATA_SOURCE=mock
EXPO_PUBLIC_API_URL=http://localhost:3000/v1
