[build]
  command = "npm install && npm run build --workspace=apps/movil"
  publish = "apps/movil/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
